/// Phase 1 stub — implemented in Phase 5.
/// Adjusts face-detection confidence thresholds and camera exposure
/// reactively based on current lighting conditions.
class LightingAdaptationService {
  bool get isActive => false;

  void evaluate() {
    // TODO(phase5): Adjust thresholds based on brightness histogram.
  }
}
