/// Phase 1 stub — implemented in Phase 2.
/// Wraps CameraSource and manages permissions, init, disposal,
/// and the image-stream subscription.
class CameraService {
  bool get isStreaming => false;

  Future<void> start() async {
    throw UnimplementedError('CameraService not yet implemented');
  }

  Future<void> stop() async {}
}
